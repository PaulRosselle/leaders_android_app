<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LeadersAndroidApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:colorControlHighlight">@color/ripple_effect</item>
    </style>

    <style name="Theme.LeadersAndroidApp" parent="Base.Theme.LeadersAndroidApp" />
</resources>